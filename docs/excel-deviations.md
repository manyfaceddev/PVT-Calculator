# Excel Deviations Ledger

Every place the Python engine deliberately differs from a source workbook.
Each entry needs: workbook + cell proof, what Excel does, what the engine does, status
(`proposed` until reviewed point-by-point with Swej, then `approved`/`parity-kept`).

| ID | Workbook / cell | Excel behavior | Engine behavior | Status |
|----|-----------------|----------------|-----------------|--------|
| D-001 | Library canonization: LiveOil v4.1 vs Flash v6.1 `Component_Properties` (C7 100.205 vs 100.204; H2S 34.082 vs 34.0809; C36+ default 635 vs 636.4) | Two variants in circulation | One canonical table (Flash v6.1 values); C36+ MW is a per-study override | proposed |
| D-002 | Engine-wide | Sheets never call their validators; silent div/0 possible (e.g. recomb calc.py P_recomb=0) | calc entry points raise `InputValidationError` (Phase 2 pattern; recombination retrofit tracked there) | proposed |
| D-015 | Engine-wide vs pre-Phase-0 app | Old recombination app used P_std=14.696 psia and CC_PER_STB=158987.1 | Canonical lab basis P_STD_PSIA=14.73 + CC_PER_STB=158987.29 per spec §6; recombination outputs shift ~0.2% vs the old app | proposed |
| D-003 | `Z factor calculation.xls` Properties!J4 | CO2 coefficient alpha2 = -0.09034 (digit transposition) | Piper-McCain-Corredor gravity form uses the published alpha2 = -0.90348 (SPE 26668, 1993); Tpc shifts ~2.5% vs the transposed value at 20% CO2 | proposed |
| D-004 | `Z factor calculation.xls` Piper-McCain-Corredor compositional form, C7+ MW cell | C7+ molecular weight is an unweighted sum/average across C7+ species | Engine mole-fraction-weights the C7+ MW: Sigma(y_i*MW_i) / Sigma(y_i), per SPE 26668 | proposed |
| D-005 | `Z factor calculation.xls` Dranchuk-Abou-Kassem Z-factor Newton iteration | Incorrect Newton derivative formula in the implicit residual with respect to Z | Engine uses mathematically correct derivative of the compressibility residual; converged Z roots remain identical (divergence only in iteration path) | proposed |
| D-006 | ADRIC CVD workbook, `Additional_QC` sheet, Hall-Yarborough Z-factor block | Uses the ordinary reduced temperature Tr (= T/Tpc) in place of the HY reciprocal t (= Tpc/T); omits the ·t factor in the A-term; returns the reduced gas density y itself as "Z" instead of A/y | Engine implements canonical Hall & Yarborough (1973): t = Tpc/T, A = 0.06125·t·Ppr·exp(−1.2(1−t)²), Newton-solves F(y)=0 for reduced density y, returns Z = A/y; matches the (separate, correct) Gas_Gradient VBA `CalculateZFactor` kernel, not this broken variant | proposed |
| D-007 | `Bubble point pressure correlations.xls` F38, Standing (1947) block | Sheet leaves the correlation exponent `a` (= 0.00091·T − 0.0125·API) as a raw user-entered cell input; never computes it from T/API itself | `pvt.correlations.bubble_point.standing.bubble_point(rs, gas_gravity, api, t_f)` computes `a` internally from T and API; `bubble_point_with_exponent(rs, gas_gravity, a)` reproduces the sheet's literal raw-`a` form exactly and is the parity hook used to verify the two never diverge (golden: `bubble_point_with_exponent(1000.0, 0.65, 0.0) == 8016.32062952945`) | proposed |
| D-008 | `Bubble point pressure correlations.xls`, Vasquez-Beggs block | Sheet computes the exponent term as a = -C3·API·(T+460) — multiplying by (T+460) instead of dividing — which overflows to #NUM! | `pvt.correlations.bubble_point.vasquez_beggs.bubble_point(rs, gas_gravity, api, t_f)` divides by (T+460), per the published Pb-form `Pb = [C1·(Rs/γg)·10^(-C3·API/(T+460))]^C2` (Vasquez & Beggs 1980; Ahmed tabulated coefficients), independently cross-checked against the original 1980 Rs-form inversion (agreement ~0.5%, within the tabulated coefficients' own rounding precision — see module docstring and `test_round_trip_against_original_rs_form`) | proposed |
| D-009 | `Bubble point pressure correlations.xls`, Glaso block (cell F81) | Sheet multiplies Pb* by a stray factor of 14.5 and never applies the final 10^log10(Pb) step (returns log10(Pb) itself, mislabeled as Pb); sheet also rounds the correlation constants to 1.767/1.745 | `pvt.correlations.bubble_point.glaso.pb_star(rs, gas_gravity, api, t_f)` computes Pb* with no stray factor — anchored exactly against the workbook's own cached F81 = 497.662528246482 (= Pb* × 14.5, dividing out the bug; see `test_pb_star_matches_workbook_cell`); `bubble_point(...)` applies the published constants 1.7669/1.7447 (vs the sheet's 1.767/1.745) and returns 10^log10(Pb), per Glaso (1980), JPT May 1980 (SPE 8016) | proposed |
| D-010 | `5_Viscosity_HPHT_Calc_v2.xlsx`, Lee-Gonzalez-Eakin gas density block | Sheet hardcodes the lbm/ft³→g/cc conversion factor as the rounded 0.0014935 | `pvt.correlations.viscosity.lee_gonzalez_eakin.gas_density_g_cc(p_psia, mw, z, t_f)` derives `DENSITY_COEF` exactly from canonical constants: 0.016018463 (lbm/ft³→g/cc, = G_PER_LB / 30.48³) / `R_PSIA_FT3_LBMOL_R` (10.7316) = 0.0014926444... ; ~0.06% lower than the sheet's rounded coefficient — golden test `test_golden_viscosity_workbook_point` (workbook cached ρ=0.04155 @965 psia, Z=0.945, M=19.5, T=256°F) asserts at rel=2e-3 to absorb the difference | proposed |
| D-011 | `gasprop_functions.bas`, `ThodosGasVisc` line `rhorv = vcmv * PresPsia / (GasZ * 10.73 * TdegR)` | VBA hardcodes the gas constant as the rounded literal 10.73 | `pvt.correlations.viscosity.jossi_stiel_thodos.reduced_density(p_psia, z, t_r, vc_mix)` uses the canonical `R_PSIA_FT3_LBMOL_R` (10.7316) per the task brief; the dense-viscosity polynomial and dilute-term branches otherwise transcribe the VBA coefficients exactly (0.1023/0.023364/0.058533/-0.040758/0.0093324/1e-4; 0.00034/0.888; 0.001668/0.1338/0.0932/(5/9)) — reduced density shifts ~0.015%, immaterial at this module's analytic test tolerances | proposed |
